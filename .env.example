# Application
APP_PORT=3000
NODE_ENV=production

# Database
POSTGRES_USER=wirwohnen
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=wirwohnen
DB_PORT=5432

# Constructed by docker-compose, but needed if running app outside Docker
DATABASE_URL=postgresql://wirwohnen:change_me_in_production@localhost:5432/wirwohnen

# Deployment target: set to "github-pages" for static export, leave unset for SSR
# DEPLOY_TARGET=github-pages
